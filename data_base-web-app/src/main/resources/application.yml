server:
  port: 5555
  servlet:
    context-path: /data_base-web-app
spring:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:airlinedb
    driver-class-name: org.h2.Driver
    username: sasha
    password: sasha
  sql:
    init:
      mode: always
